[phases.setup]
nixPkgs = ["nodejs_18", "python311"]

[phases.install]
cmds = ["npm install", "cd frontend && npm install", "cd backend && npm install", "cd ai-service && pip install -r requirements.txt"]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "npm start"
